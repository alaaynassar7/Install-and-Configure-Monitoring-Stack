groups:
- name: node_alerts
  rules:
  - alert: HighCPUUsage
    expr: rate(node_cpu_seconds_total{mode="user"}[1m]) > 0.8
    for: 1m
    labels:
      severity: warning
    annotations:
      summary: High CPU usage detected

